version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    branches:
      only:
        - master
    steps:
      - checkout

      - run:
          command: |
            yarn add sync-glitch-cli -D --ignore-engines
            yarn sync
